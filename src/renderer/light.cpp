#include<renderer/light.h>
namespace FS {

	void Light::updateLight() {
		//glm::mat4 rot = lightTrackBall->getRot();
		//glm::vec3 temp = glm::normalize(glm::vec3(1.0, 1.0, 1.0));
		//temp[0] = temp[2] = temp[2] = temp[0] * 2;
		//pos = glm::vec3(rot* glm::vec4( temp,0));


	}

}